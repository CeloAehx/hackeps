# URL de conexión a MongoDB
# Reemplaza <username>, <password> y <cluster> con tus credenciales reales
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/mejor-barrio?retryWrites=true&w=majority

# URL base para la API (opcional, por defecto usa relativa)
NEXT_PUBLIC_API_URL=http://localhost:3000/api
